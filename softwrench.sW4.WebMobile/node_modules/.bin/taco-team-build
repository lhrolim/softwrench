#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../taco-team-build/bin/taco-team-build" "$@"
  ret=$?
else 
  node  "$basedir/../taco-team-build/bin/taco-team-build" "$@"
  ret=$?
fi
exit $ret
